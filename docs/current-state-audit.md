# 当前代码状态审计（自媒体内容拆解工作台）

## 已完成

- 对标账号、作品、公开互动快照和热度检测结果的数据模型。
- 作品发布后 0–48 小时每 4 小时、第 3–7 天每天一次、第 7 天最终快照的策略函数。
- 基于账号最近 20 条历史作品、使用实际可用公开互动指标的相对热度检测。
- `hot` 进入分析队列，`very_hot` 仅提高处理顺序。
- 账号导入、抖音作品分享链接解析作者 `sec_uid`、作品观察页和队列接口。
- 工作台已接入 OpenCLI Admin 的账号绑定、手动检查和 `hot`/`very_hot` 待分析队列。
- 工作台已接入可选的 video-jiexi HTTP 提供方：解析、下载进度和发布中心交接。
- 工作台已提供外部服务配置页，地址和 Token 不再写死在代码或个人路径中。

## 可复用

- OpenCLI 数据源、任务执行器、Cron 调度和浏览器登录态基础设施。
- 内容身份去重、时序快照、公开计数解析、检测证据和分页查询。
- 工作台账号绑定、手动检查和作品拉取契约。

## 已真实验证

- 抖音 `user-videos <sec_uid>` 的真实账号作品采集。
- 作品 ID、稳定链接、发布时间、点赞、收藏、评论和分享字段入库。
- 同一作品的多次数据快照，以及最近 20 条账号基线。
- 一条达到 2.3 倍相对热度的真实作品进入待分析队列。
- 待分析作品链接成功交给本机 video-jiexi 并返回标题、作者、封面和画质。
- 真实视频完成下载并作为 14.05 MB 待发布文件进入发布中心。

## 仍需真实验证

- 结合视频、文案和评论的爆款拆解与二创脚本生成。
- 使用真实发布账号进行一次手动确认后的发布。
- 远程部署 video-jiexi 时的 Token 鉴权和文件传输。
- 更多真实错误样本下的 `account_invalid`、`login_required`、`login_expired` 分类。

## 暂不处理

- OpenCLI Admin 独立运营中台和主 UI 扩展。
- 飞书运行时、通知、自动发布、自动评论和复杂多机编排。
- Cheat on Content 强依赖、账号定位、长期偏好和自动个性化学习。
- 对标作品完播率；该指标只属于自有账号反馈层。
